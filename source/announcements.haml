---
title: Announcements
---

- posts = EssgeeSoccer::Post.where(type: 'announcement').order_by_date(:asc)

- if posts.empty?
  %p.text-center None.
- else
  - posts.each do |post|
    = partial 'post', locals: { post: post }
