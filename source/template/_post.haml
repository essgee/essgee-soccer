.row
  .columns.small-12
    .row
      .columns.small-12
        .post{ class: "post-#{post['type']}" }
          .header
            %p= header(post)
          .content
            = date(post)
            = content(post)
